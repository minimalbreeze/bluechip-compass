# 블루칩 나침반 — 제품 원칙 (최상위, 절대 흔들리지 않음)

이 문서의 내용은 다른 모든 기능 요청보다 우선한다. 새 기능을 추가하거나 기존
기능을 바꿀 때, 아래 원칙과 충돌한다면 그 요청 쪽을 다시 확인하고 이 문서를
따른다.

## 제품 정체성

| 항목 | 내용 |
|---|---|
| 앱 | 블루칩 나침반 |
| 성격 | **투자 교육 자료**. 자문·추천 서비스가 아니다 |
| 다루는 질문 | "50년 뒤에도 이 회사가 있을까" |
| 다루지 않는 질문 | "얼마나 오를까", "언제 사야 하나" |
| 핵심 UX | 홈(시장 + 내 현황) → 내 주식 → 제안 → 시장 → 자료 |
| 다루는 것 | **개별 블루칩 종목만.** ETF·펀드·파생은 다루지 않는다 |
| 대상 | 처음 시작하는 사람. 모든 문장은 그 사람이 읽는다는 전제로 쓴다 |

## 최상위 원칙

1. **추천하지 않는다. 판단하는 법을 준다.**
   특정 종목의 매수·매도를 권유하지 않고 어떤 수익도 보장하지 않는다.
   교육 자료 고지는 **첫 방문 배너 · "내 배분" 결과 하단 · "배우기" 탭 하단**
   세 곳에 반복 노출한다. **줄이거나 없애지 않는다.**

2. **매일 바뀌는 숫자를 파일에 넣지 않는다.**
   주가·시가총액·PER·PBR·배당수익률·분기 실적은 하드코딩 금지. 빌드도 서버도
   없는 정적 앱이라 넣는 순간부터 틀린 값이 되고, 초보자는 그 틀린 값을 믿는다.
   대신 종목마다 `check`(확인할 지표의 **이름**)를 주고, 시장별 `sources`로
   원자료 링크(DART·KRX·한국은행 / SEC EDGAR·FRED·FedWatch)를 건다.

3. **시황을 문장으로 적지 않는다. 판정 규칙으로 만든다.**
   "지금 시장은 이렇습니다"를 적으면 배포 다음 날부터 거짓말이 된다.
   5개 항목(금리·경기·밸류에이션·환율·지정학)을 **실제 수치에서 판정해**
   국면·배분·행동을 다시 계산한다. 파일이 낡아도 결과는 오늘 기준이 된다.
   판정은 워크플로가 서버에서 하고 앱은 결과를 읽는다(§16).
   `market.js`의 `DEFAULTS`는 판정을 못 받아왔을 때만 쓰이는 **비상 출발값**
   이다. 고칠 때는 **`asOf`도 반드시 같이 고친다.**

4. **개별 회사만 다룬다. ETF는 넣지 않는다.**
   이 앱은 "이 회사가 50년 뒤에도 있을까"를 회사 단위로 따지는 도구다. 지수를
   통째로 사는 선택지를 섞으면 그 질문 자체가 흐려진다. 모델 포트폴리오·유니버스·
   보유 입력 어디에도 ETF를 넣지 않는다(용어 사전의 설명 항목은 예외).

5. **존속 가능성만 평가한다. 수익률을 예측하지 않는다.**
   6축(수요 지속성·해자·재무 체력·현금창출력·주주환원 지속성·지정학 내성)은
   "안 망할까"를 묻는 축이다. 목표주가·기대수익률·매수 시점을 계산하는 기능은
   추가하지 않는다. 점수는 **정성 평가**이며 UI에도 그렇게 표시한다.
   점수를 고칠 때는 근거(`why`)와 리스크(`risk`)를 같이 고친다.

6. **초보자를 다치지 않게 하는 쪽으로 기운다.**
   - 국면에 따른 배분 조정은 **비대칭**이다. 위험을 키우는 방향(+위성)보다
     줄이는 방향(+현금)의 절댓값이 크다. 초보자에게는 기회를 놓치는 것보다
     크게 다치는 쪽이 훨씬 비싸다.
   - "3년 안에 쓸 돈"을 고른 사용자에게는 계산 결과보다 경고를 먼저 보여준다.
   - 변동이 큰 종목은 `satellite`로 분류해 위성 슬롯에만 넣고, 카드에 경고
     태그를 붙인다.
   - 어려운 단어에는 점선 밑줄을 달아 탭하면 설명이 뜨게 한다. 사용자가 모르는
     단어에서 멈추고 나가버리지 않게 한다.

## 7. 홈은 사용자가 고른 위젯으로 만든다

홈에 뭘 올릴지는 사람마다 다르다. 보유 종목이 없는 사람에게 "내 투자 현황"은
빈칸이고, 뉴스를 안 보고 싶은 사람에게 뉴스는 소음이다. 그래서

- 위젯 단위로 **켜고 끈다**(⚙️ 편집 패널)
- 모든 카테고리는 **접었다 폈다** 한다
- 두 선택 모두 `bcc:widgets` / `bcc:folds` 에 저장된다

새 콘텐츠를 홈에 넣고 싶을 때는 `WIDGETS` 에 항목을 더하고 렌더 함수를 하나
만든다. **끄면 완전히 사라져야 하고**, 꺼도 그 내용은 해당 탭에서 볼 수 있어야
한다(홈은 요약이지 유일한 통로가 아니다).

예외가 하나 있다: **시장 확인 신선도 줄은 위젯이 아니다.** 이 앱의 판단이 며칠
된 값에 기대고 있는지는 끌 수 있으면 안 된다.

자주 쓰지 않는 입력(종목 추가 폼 등)은 기본으로 숨기고 버튼 뒤에 둔다.
늘 펼쳐 두면 정작 자주 보는 내용이 아래로 밀린다.

## 8. 뉴스는 매매 버튼이 아니다

뉴스 위젯은 헤드라인과 출처, 링크를 주고, 기사마다 **판정 한 줄**을 붙인다.
요약하거나 "이래서 오늘 시장이 이렇습니다" 식으로 엮지 않는다 — 그건 이 앱이
확인할 수 없는 인과관계다.

판정은 `scripts/judge-news.mjs`가 워크플로에서 하고(§16과 같은 구조: 키가 없으면
키워드 규칙, 있으면 AI), **AI 판정만** 링크로 캐시한다 — 규칙은 다시 계산해도
공짜인데 캐시에 넣으면 규칙을 고쳐도 예전 판정이 남는다. 캐시의 목적은 API
호출을 아끼는 것이지 계산을 아끼는 게 아니다.

**판정값은 셋뿐이고, 어느 것도 매매 지시가 아니다.**

| 값 | 뜻 |
|---|---|
| `none` ✅ 오늘 할 일 없음 | 이미 가격에 반영됐을 이야기. 대부분이 여기다 |
| `watch` 👀 지켜보기 | 구조를 바꿀 수 있는 사건. 기억만 하고 오늘은 아무것도 안 한다 |
| `review` 🔍 근거 다시 확인 | **파는 게 아니라** 처음 산 이유를 다시 읽으라는 뜻 |

지켜야 할 것.

- **"파세요·사세요"를 판정에 넣지 않는다.** `validateNews()`가 그런 문장을
  걸러 기본 판정으로 되돌린다. 이 방어를 없애지 않는다.
- 판정문(`why`)은 **AI 출력이 들어오는 칸**이다. 화면에서 항상 `esc`로
  이스케이프하고, 그래서 규칙이 만드는 문장에도 HTML 태그를 쓰지 않는다.
- 종목을 지목하지 않는다. `scope`(시장/업종/개별 회사)로만 말한다.
- 목록 위에 **오늘 전체 요약 한 줄**을 둔다. 여섯 건을 다 읽지 않아도
  "오늘은 할 일이 없다"를 먼저 알 수 있어야 한다.
- 판정 주체(🤖 AI / 📐 규칙)를 요약 줄에 밝힌다.
- 3문항 자가점검은 없애지 않고 **판정 기준으로 공개한다**(자료 탭). 어떤
  잣대로 판정했는지 모르면 사용자가 그 판정을 검증할 수 없다.

미국 기사는 **한국어로 번역해서 보여주되 원문을 함께 남긴다.** 기계 번역은
금융 헤드라인의 뜻을 뒤집을 때가 있어서("beat expectations" 같은 표현),
번역만 보여주면 사용자가 확인할 방법이 없어진다. 번역문임을 화면에 밝히고,
번역에 실패한 기사는 원문 그대로 내보낸다 — 빈칸으로 두지 않는다.

뉴스 제목·링크는 **남의 서버에서 온 문자열**이다. 수집 단계에서 태그를 벗기고
꺾쇠를 제거하며(`scripts/fetch-live.mjs`), 렌더 단계에서 다시 이스케이프하고
(`esc`) 링크 스킴을 확인한다(`safeUrl`). 이 이중 방어를 줄이지 않는다.

## 9. 첫 화면은 결론이다

탭 순서 `홈 → 내 주식 → 제안 → 시장 → 자료`. 홈은 두 가지에만 답한다:
**지금 시장이 어떤가**, **내 돈이 지금 어떤가**. 공부는 맨 뒤에 있다.

초보자는 공부하다 지쳐 아무것도 못 사거나, 공부를 건너뛰고 아무거나 사거나
둘 중 하나가 된다. 이 앱은 **결론을 먼저 주고 근거를 뒤에 붙이는** 쪽을 택했다.
교육 콘텐츠를 앞으로 옮기거나, 홈에 읽을거리를 늘리는 변경은 하지 않는다.

홈에 무언가를 추가하려 할 때 기준: 그것이 **오늘의 행동을 바꾸는가**.
바꾸지 않으면 "자료"로 보낸다.

## 10. 모의투자는 앱의 판단을 검증하는 자리다

"내 주식"이 실제로 들고 있는 것이라면, 모의투자는 **이 앱이 제안한 배분을
그대로 굴린 것**이다. 둘을 나란히 놓고 비교하는 게 목적이다 — 앱의 판단이
실제로 쓸모가 있었는지를 사용자가 스스로 확인할 수 있어야 한다.

**그래서 모의투자는 자동으로 굴러간다.** 한 번 담아놓고 방치한 계좌는 앱의
판단이 아니라 과거의 판단이고, 그걸 비교해봐야 아무것도 알 수 없다.

- 목표는 **지금 국면으로 다시 계산한 모델 배분**(`P.build`)이다. 국면이
  바뀌면 현금 비중이 움직이고 종목 비중도 따라 움직인다.
- 목표에서 `BAND`(3%p) 넘게 벌어진 자리만, **하루 한 번까지만** 조정한다.
  잔손질을 반복하면 이 앱이 줄이려는 행동을 앱이 하게 된다.
- ⚠️ **자리마다 밴드를 걸면 현금이 조용히 어긋난다.** 일곱 자리가 각각 밴드
  안이어도 모이면 현금이 목표에서 20%p 벗어난다. 그런데 국면이 실제로
  움직이는 값이 바로 그 현금 비중이다. 그래서 **현금이 밴드를 벗어난 날은
  밴드를 풀고 전 자리를 목표로 맞춘다.** 이 보정을 지우지 않는다.
- **거래마다 왜 그렇게 했는지를 남긴다**(`log[].why`, 자동은 `auto: true`).
  근거 없이 잔고가 바뀌면 사용자는 그 계좌를 이해할 수 없다.
- **끌 수 있어야 한다.** 자동 운용은 기본값이지 강제가 아니다. 껐을 때도
  "지금 목표와 몇 자리가 벌어져 있는지"는 보여준다.

비교를 보여줄 때는 **기간이 다르다는 사실을 반드시 같이 적는다.** 모의투자는
시작일에 담아 자동 조정해온 결과이고 실제 보유는 종목마다 산 시점이 다르므로,
수익률 차이를 곧바로 우열로 읽으면 안 된다. 그래서 손익만큼이나 시작일·성향·거래 내역을 같이
보여준다 — 내가 언제 무엇을 팔았는지가 남아야 배운다.

연습이라는 사실을 화면에서 숨기지 않는다. 매 화면 하단에 넷을 적는다.

1. 체결가는 **30분 스냅샷 가격**이라 실제 체결가와 다르다
2. **수수료·세금·슬리피지가 없다** — 실제로는 그만큼 덜 남는다
3. 배당이 없다
4. (미국) 달러 시세를 그때그때 환율로 환산하므로 주가가 그대로여도 환율로
   평가액이 바뀐다 — 한국에서 미국 주식을 사면 실제로 그렇다

**순위표·수익률 경쟁·연승 배지 같은 장치를 만들지 않는다.** 그건 이 앱이
줄이려는 바로 그 행동을 부추긴다. 시세가 없는 종목은 매수 원가로 표시하고
"시세 없음"을 붙인다 — 0으로 처리하면 손실이 난 것처럼 보인다.

## 11. 매일 바뀌는 척하지 않는다

이 앱은 주가를 받아오지 않는다. 그러니 **종목 목록이 매일 저절로 바뀌는 기능을
만들지 않는다.** 날짜를 시드로 종목을 섞거나, 순서를 돌리거나, "오늘의 추천
종목"을 난수로 뽑는 구현은 금지다 — 근거 없는 변화를 시장 신호처럼 보이게
만드는 순간 이 앱은 거짓말을 시작한다.

매일 달라져도 되는 것은 딱 둘이다.

1. **실제 수치에서 나온 시장 국면 판정** — 진짜 입력이라 진짜 근거다.
   판정 시각과 판정 주체(AI/규칙/직접)를 화면에 항상 표시한다.
2. **오늘의 점검 한 가지** — 날짜에서 나오는 행동 제안. 시장 데이터인 척하지
   않는다.

**지수 수치는 `live.json` 에서만 온다.** 소스 파일에 손으로 적어 넣지 않는다.
`live.json` 은 GitHub Actions(`.github/workflows/live.yml`)가 주기적으로 받아
커밋하는 **스냅샷**이며, 실시간이 아니다. 그래서 화면에는 값과 함께 반드시
"몇 분 전 값"인지 표시한다 — 실시간인 척하지 않는다. 시세를 못 받아온 상태에서
앱이 깨지지 않아야 하고(링크만 보여준다), 값이 하나도 없으면 갱신 시각 문구도
띄우지 않는다(빈 스냅샷에 시각만 붙으면 값이 있는 것처럼 읽힌다).

판단은 자동으로 바뀌지만, **왜 그렇게 판정했는지는 항상 같이 낸다**(§16).
근거 없이 값만 바뀌면 사용자가 검증할 수 없고, 그건 "그냥 믿으세요"와 같다.

## 12. 보유 판단에 손익을 쓰지 않는다

"내 주식" 화면은 팔지·둘지·더 살지를 제안한다. 그 판단의 근거는 둘뿐이다.

- **목표 비중과의 차이** (사용자가 고른 성향 + 사용자가 맞춘 국면에서 나온 값)
- **50년 점수** (이 앱의 정성 평가)

**수익률은 화면에 보여주기만 하고 판단에 넣지 않는다.** "많이 떨어졌으니
팔아라"도 "많이 올랐으니 팔아라"도 둘 다 틀렸다. 팔지 말지를 정하는 건
처음 산 이유가 아직 유효한가이지 지금 손익이 아니다. `holdings.js`의 판정
규칙에 `ret`이 들어가는 순간 이 원칙이 깨진다.

시세를 조회하지 않는다는 사실을 화면에서 숨기지 않는다 — 손익은 사용자가
입력한 값으로 계산한 것이라고 매번 밝힌다.

진짜 자동 갱신이 필요하면 서버(Cloudflare Worker 등)를 붙이는 게 정답이다.
README의 "진짜 매일 갱신하려면" 참고.

## 13. 보유 입력은 단가와 수량으로 받는다

"매수금액 + 수익률"을 받으면 사용자가 증권사 앱에서 수익률을 읽어와야 하고,
그 값이 언제 기준인지 알 수 없다. **주당 매수단가와 수량은 한 번 적으면 안
바뀌는 사실**이고, 수익률은 이 앱이 받아온 시세로 직접 계산하면 된다.

금액은 그 시장 통화의 기본 단위로 다룬다 — 국내는 원, 미국은 달러. 원화로
합칠 때만 환율을 곱한다. 그래야 "달러로 보기 / 원화로 보기"를 자유롭게 오갈
수 있다.

미국 종목은 **매수 시점 환율(`fxAt`)** 을 같이 저장하고, 원화 손익을
**주가 기여분과 환율 기여분으로 쪼개서** 보여준다. 이걸 안 나누면 "주가는
올랐는데 왜 원화로는 손해지?" 하는 순간을 설명할 수 없다.

입력 방식을 또 바꾸게 되더라도 **기존 데이터를 버리지 않는다.** 예전 방식으로
들어온 항목은 그대로 계산해 보여주고, 새 방식으로 다시 넣으라고 안내만 한다.

## 14. 종목 입력은 이름을 맞춰주되, 막지는 않는다

보유를 등록할 때 국내·미국 **상장 종목 전체 목록**에서 이름을 자동완성한다
(`tickers.json`, 워크플로가 만든다). 사용자가 종목명을 정확히 외우고 있을
이유가 없고, 이름이 틀리면 티커가 안 붙어 시세도 판정도 못 붙는다.

- 검색 결과에서 **유니버스(50년 카드) 종목을 항상 위에** 올린다. 이 앱이
  실제로 평가하고 시세도 갖고 있는 종목이라 가장 쓸모 있다.
- 목록에 없는 이름도 **직접 적을 수 있어야 한다.** 상장 목록이 낡았거나
  해외 소형주라 빠져 있을 수 있고, 그때 입력을 막으면 앱이 쓸모없어진다.
- 시세를 받아오지 않는 종목은 **현재가를 직접 적는 칸**을 준다. 비워두면
  매수 원가로 표시한다. 자동 시세가 있으면 그쪽이 항상 이긴다 — 사람이
  적어둔 값은 낡기 때문이다.
- 색인은 수백 KB라 **종목 추가 폼을 처음 열 때만** 내려받는다. 못 받아와도
  유니버스 검색은 그대로 동작해야 한다.

## 15. 사용자가 확인할 것을 늘리지 않는다 — 판정은 앱이 한다

이 앱은 처음에 사용자에게 확인을 요구하는 화면이 셋이었다. 시장 다이얼 5개,
기사마다 3문항 자가점검, 모의투자의 수동 매매. 셋 다 "배우게 하려고" 넣었지만,
**확인을 요구하는 순간 아무도 하지 않았고** 그래서 앱 전체가 낡은 값과 방치된
계좌로 굴러갔다.

그래서 원칙을 뒤집었다. **판정은 앱이 하고, 사용자는 결과와 근거를 읽는다.**
새 기능을 넣을 때 "사용자가 매일/매번 무언가를 확인해야 하는가"를 먼저 묻고,
그렇다면 자동으로 판정할 방법부터 찾는다. 자동화할 수 없으면 그 기능을 넣지 않는
쪽이, 아무도 안 하는 확인 화면을 하나 더 만드는 것보다 낫다.

대신 자동화한 것마다 **근거를 반드시 함께 낸다.** 값만 바뀌고 왜 그런지 말하지
못하면 "그냥 믿으세요"가 되고, 그건 이 앱이 가장 하지 않으려는 것이다.
그리고 **직접 고치는 길을 항상 남긴다** — 자동 판정은 출발점이지 결론이 아니다.

## 16. 시장 국면은 자동으로 판정하되, 근거를 반드시 같이 낸다

예전에는 사용자가 다이얼 5개를 직접 맞춰야 했다. "무엇을 봐야 하는지"를 배우는
효과는 있었지만, **매일 확인하라는 요구 자체가 부담**이라 결국 아무도 확인하지
않았고 앱 전체가 낡은 값으로 굴러갔다. 그래서 판정을 서버로 옮겼다.

- 판정은 `scripts/judge-regime.mjs`가 하고, 결과는 `live.json` 의 `regime`
  키에 들어간다. 앱은 그 값을 읽기만 한다.
- 두 갈래가 있고, **키가 없어도 항상 판정이 나온다.**
  - `judgeByRules()` — 실제 수치에서 기계적으로 도출. 금리는 미국 10년물
    (`^TNX`)의 3개월 방향 + 1년 범위 위치, 경기는 지수의 3개월 변화,
    밸류에이션은 지수의 1년 범위 위치, 환율은 원/달러의 1년 범위 위치,
    지정학은 VIX. 방향만 보면 1년 최고 구간에 눌러앉은 금리를 "멈춰 있음"
    으로 읽으므로, 방향이 완만해도 극단에 있으면 그 극단 쪽으로 판정한다.
  - `judgeByAI()` — `ANTHROPIC_API_KEY` 가 있을 때만. 같은 수치에 헤드라인을
    얹어 읽고 판정한다. 규칙이 못 보는 맥락(관세 발표, 정책 전환)을 잡는다.
    실패하면 **조용히 규칙 판정으로 돌아간다.** AI가 죽어도 앱은 멀쩡해야 한다.
- 모델이 낸 값은 `validate()`로 선택지 안에 있는지 확인하고, 벗어나면 규칙 값을
  쓴다. 자유 문자열을 그대로 화면에 흘리지 않는다.

**지켜야 할 것 — 이게 이 원칙의 핵심이다.**

1. **판정마다 한국어 근거 한 줄**(`why`)을 붙인다. 근거에는 판정에 쓴 **수치를
   인용**한다. 값만 바뀌고 왜 그런지 말하지 못하면 "그냥 믿으세요"가 된다.
2. **판정 주체를 항상 표시한다** — 🤖 AI / 📐 규칙 / ✍️ 직접 고침. 홈과 시장 탭
   모두. 어떤 근거에 기대고 있는지는 숨기지 않는다.
3. **금리·경기·밸류에이션은 대용 지표다.** 정책금리도, 경기지표도, PER도 아니다.
   그렇게 화면에 적는다. 대용 지표를 진짜 지표인 척 쓰지 않는다.
4. **직접 고치는 길을 항상 남긴다.** 자동 판정은 출발점이지 결론이 아니다.
   하나라도 고치면 그 시장은 `regimeMode: 'manual'` 이 되고, 이후 자동 판정이
   갱신돼도 덮어쓰지 않는다. 되돌리기는 사용자가 누를 때만 일어난다.
5. 직접 고치기로 넘어갈 때 **나머지 항목은 화면에 보이던 자동 판정 값을 물려받는다.**
   하나 고쳤다고 나머지가 옛 기본값으로 돌아가면 사용자가 놀란다.
6. AI에게 **종목 추천·매수 시점·수익률 예측을 시키지 않는다.** 지금 상태를
   정해진 선택지로 분류하는 것이 전부다(§1, §5와 같은 선).


## 17. 시세를 받아오는 것과 평가하는 것은 다르다

`scripts/price-list.mjs` 는 **시세만** 받아오는 목록이다(약 370종목).
이 앱이 "50년 뒤에도 있을까"를 채점하는 종목은 `data.js` 의 유니버스 13+13
뿐이고, 그건 늘리지 않는다. 두 가지를 섞지 않으려고 파일을 나눴다.

왜 필요했나 — 워크플로는 사용자의 보유 종목을 모른다(브라우저 밖으로 내보내지
않기 때문이다). 그래서 목록에 없는 종목은 화면에서 **손익이 0원에 멈춘다.**
실제로 "미국 보유 종목의 변화가 안 보인다"는 말을 듣고서야 알았다.

- 전부(15,000종목 이상) 받지 않는다. Yahoo 가 막으면 지금 되는 것마저 잃고,
  파일이 1.8MB 가 되어 앱을 열 때마다 휴대폰이 그걸 내려받는다.
- `prices.json` 은 **보유 종목이나 모의투자 계좌가 있을 때만** 내려받는다.
  아무것도 등록하지 않은 사람까지 그 무게를 질 이유가 없다.
- 갱신은 하루 두 번(장 마감 뒤). 유니버스 26종목만 30분마다 받는다 —
  자기 종목을 30분 단위로 들여다보지 말라는 게 이 앱이 하는 말이다.
- 목록을 고치면 **반드시 `node scripts/check-list.mjs` 로 전수 검증한다.**
  상장폐지·피인수로 사라진 티커가 섞이면 매번 헛되이 두드리게 된다.
  실제로 처음 작성한 381건 중 9건이 그런 경우였다.
- 목록에 없는 종목은 화면에서 **⚠️ 시세 없음 · 손익이 움직이지 않습니다**
  라고 분명히 밝힌다. 조용히 매수 원가를 찍어두면 사용자는 그게 실제 평가액인
  줄 안다.

## 18. 검색은 사용자가 고르려던 것을 첫 줄에 올린다

자동완성에서 "Intel"을 치면 `Intelligent Alpha Atlas ETF` 가 첫 줄에 왔었다.
이름이 질의로 시작하는 것을 전부 한 묶음에 넣고 색인 순서(알파벳 티커 순)로
보여준 탓이다. 그걸 고르면 **엉뚱한 종목이 등록되고**, 티커가 다르니 시세도
판정도 안 붙는다.

- 점수로 정렬한다: 티커 정확 일치 → 이름 정확 일치 → 이름이 질의로 시작하되
  거기서 **낱말이 끝나는 것**("Intel Corporation") → 티커 시작 → 이름 시작
  ("Intelligent") → 이름 중간 포함.
- ETF 는 같은 점수대에서 반 칸 뒤로 민다. 이 앱이 다루는 대상은 아니지만
  들고 있을 수는 있어서 지우지는 않는다. 다만 **배율·인버스 상품은 두 칸**
  뒤로 민다. "SK"를 치면 티커가 정확히 일치하는 `Corgi SK hynix 2x Daily ETF`
  가 1등이었다 — 초보자가 그걸 고르면 원주 대신 2배 ETF 가 등록된다.
- KRX 공식 약칭이 영문인 종목("NAVER")은 한글로도 찾히게 별칭을 둔다.
  안 나오면 사용자는 이름을 직접 적어버리고, 그러면 티커가 안 붙는다.
- **미국에 ADR 로 상장된 한국 기업도 한글로 찾힌다**(`US_KOR`). 이름이 영문뿐
  이라 "하이닉스"로는 한 줄도 안 나왔다. 그리고 **찾히는 종목은 시세도 받는다** —
  `US_KOR` 에 넣으면 `scripts/price-list.mjs` 에도 같이 넣는다. 찾히는데 시세가
  없으면 평단만 보이고 손익이 멈춘 채로 남는다(§17).

## 19. 판단은 금액으로 말한다 — 다만 산수까지만

판정이 온통 "오늘 할 일 없음"으로 보인다는 지적을 받았다. 뜯어보니 판정이
짜서가 아니라 **두 가지가 빠져서**였다.

1. 판단이 말로만 끝났다 — "일부를 덜어 목표에 맞추면". 그래서 얼마인지는
   사용자가 직접 계산해야 했고, 아무도 하지 않는다.
2. **기사와 보유 종목이 아예 연결돼 있지 않았다.** SK하이닉스를 98% 들고 있는
   사람과 한 주도 없는 사람에게 같은 기사가 같은 무게일 수 없다.

### 할 수 있는 말과 없는 말

**금액을 말한다.** 이건 예측이 아니라 **산수**다 — 사용자가 고른 목표 비중과
지금 비중의 차이를 금액·주수로 바꾼 것뿐이다.

> 한 종목이 98.7%입니다. 집중 상한 25%까지 73.7%p 초과 —
> 여기에 해당하는 금액은 8,143만원(388주)입니다.

**미래를 말하지 않는다.** "이 뉴스 때문에 30% 파세요"는 못 한다. 기사가
주가를 어떻게 만들지 이 앱은 모르고, 안다고 말하는 순간 거짓말이 된다.
그래서 금액은 **기사가 아니라 사용자의 목표 비중**에서만 나온다.

- 판정에 붙는 숫자는 `holdings.js` 의 `actionOf()` 가 낸다. `judge()` 와 달리
  **평가 못 한 종목에도 계산한다** — 한 종목이 98%인 건 이 앱이 그 회사를
  아는지와 무관한 사실이다. 여태 이런 종목은 숫자가 한 개도 없었다.
- 기사↔보유 대조는 **브라우저에서** 한다(`newsHits`). 보유 종목은 기기 밖으로
  나가지 않으므로 워크플로는 이 대조를 대신 해 줄 수 없다.
- 영문 별칭은 **낱말 경계**로 본다. 포함으로 보면 "Intelligent Alpha Atlas
  ETF"가 'intel'에 걸려 인텔 기사가 된다(§18과 같은 실수). 한글은 조사가
  붙어 다녀서 포함으로 본다.

### 물타기는 계산해 주되 권하지 않는다

실수 목록에 "손실 난 종목을 물타기로 키운다"가 그대로 들어 있다. 그런데
"권하지 않는다"만 적어두면 사용자는 감으로 넣는다. 그래서 **비용 청구서**를
보여준다 — 권유가 아니라 얼마가 드는지다.

> 1.1억을 더 넣으면 → 평단 $160 → $157.65 (−1.5%), 비중 98.7% → **99.4%**

처음엔 반대로 계산했다("평단 5% 낮추려면 얼마?"). 현재가가 평단보다 2.9%밖에
안 낮으면 5%는 수학적으로 불가능해서 대부분 "계산 불가"만 나왔다. 방향을
뒤집었다 — **얼마를 넣으면 평단이 어디까지 내려가는가.** 항상 답이 있고,
"원금만큼 넣어도 1.5%"라는 사실이 그대로 보인다.

**표 밑의 경고는 지우지 않는다.** 평단이 내려가도 비중은 올라간다는 문장이
이 표의 존재 이유다.

## 20. 기록은 있는 것만으로 부족하다 — 바뀐 것을 먼저 보여준다

"모의투자는 어디에 사고팔고 기록이 남아서 볼 수 있냐"는 물음을 받았다.
장부(`📒 매매 장부`)도 전체 내역(`🧾 전체 거래 내역`)도 진작 있었다. 문제는
**둘 다 접혀 있었다**는 것이다. 기록이 없던 게 아니라 보이지 않았다.

자동 운용은 사용자가 없는 사이에 사고판다. 그래서 앱을 열었을 때 "내가 자리를
비운 사이 무슨 일이 있었나"가 **먼저** 보여야 한다. 접힌 칸을 열어봐야 알 수
있다면, 따라서 투자할 수 없다.

- 새로 생긴 거래는 `simNewHtml()` 이 **접지 않고** 위에 편다. 무엇을 언제
  얼마에 사고팔았는지와 그 이유를 한 줄씩 보여준다.
- 홈 위젯 뱃지에도 건수를 띄운다(`🆕 2건`). 탭을 열기 전에 알아야 한다.
- **한 번 본 뒤에는 사라진다.** 매번 같은 걸 보여주면 그것도 곧 배경이 된다.
- 판 게 있으면 매매 장부는 **펴진 채로** 연다.

### 길어진 기록은 최근 하루만 편다

기록을 지우지 않는다는 원칙 때문에 장부는 계속 길어진다. 그대로 다 쏟으면
읽을 수가 없어서 결국 안 보게 된다. 그래서 **날짜로 묶고, 가장 최근 하루만**
펴 두고 나머지는 `📂 지난 기록 더 보기` 버튼 뒤에 둔다(`logFoldHtml`).

- 날짜 머리글은 사람 말로 쓴다 — `오늘`, `어제`, `3일 전`, 그 밖은 `8월 22일`.
  `2026-08-22`보다 먼저 읽힌다. 정확한 날짜는 그 옆에 작게 같이 둔다.
- 정렬을 **배열 순서에 맡기지 않는다**(`logSorted`). 시작 배분은 push, 그 뒤
  매매는 unshift 로 들어가 순서가 한결같지 않다. 날짜 내림차순으로 정렬하되
  같은 날이면 원래 자리로 가른다 — `ts` 가 하루 단위라 날짜만으로는 같은 날
  여러 건을 못 가린다.
- 하루 안에서는 자르지 않는다. "왜 오늘 것도 다 안 보이지"가 되기 때문이다.
- 펼침 상태는 기록마다 따로 기억한다(`state.logMore`). 장부를 폈다고 전체
  내역까지 펴지지 않는다.

### 날짜가 아니라 건수로 센다

`ts` 는 하루 단위라 같은 날 두 건이 생기면 날짜로는 구분되지 않는다. 로그는
줄어들지 않으므로(초기화 전까지 한 줄도 지우지 않는다) **건수 차이가 곧 새
거래**다. `sim.seen` 에 마지막으로 본 시점의 건수를 적어둔다. 초기화로 로그가
짧아지는 경우를 대비해 읽을 때 `min(seen, log.length)` 로 자른다.

계산 결과는 그 방문 동안 `simFresh` 에 붙들어 둔다. 안 그러면 접기 하나만
눌러도 다시 그려지면서 "바뀐 것" 칸이 사라진다.

## 21. 종목 해설 — 답할 수 있는 것과 없는 것을 갈라 둔다

"궁금한 주식을 치면 뭐하는 회사인지, 재무는 어떤지, 미래에 어떨지, 추천인지
비추천인지 알려달라"는 요청에서 나온 화면(`🔎 알아보기`).

### 답한다

| 물음 | 어떻게 답하나 |
|---|---|
| 뭐하는 회사인가 | `one` + `how` (돈을 어떻게 버는지) |
| 지금 어떤 상태인가 | 6축 점수 + `why`, 시세는 별도로 |
| 미래에 어떨 것인가 | **50년 뒤에도 이 회사가 있을까**로 답한다 |
| 얼마나 좋은 회사인가 | 등급 S/A/B/C — 유니버스 13종목과 **같은 잣대** |
| 무엇이 위험한가 | `risk` (구조적 위험) |
| 재무는 어떤가 | `balance`·`cash` 축 + **확인할 지표의 이름** + DART·SEC 링크 |

### 답하지 않는다 — 그리고 그 이유

- **매수·매도 의견.** 최상위 원칙 1. 프롬프트에 금지어를 적고, 그래도 나오면
  `validateOne()` 이 **그 종목을 통째로 버린다.** 반쯤 어긴 걸 고쳐 내보내는
  것보다 없는 게 낫다.
- **PER·부채비율 같은 수치.** 원칙 2. 이 파일은 오래 남고 수치는 금방 낡는다.
  빌드도 서버도 없어 고칠 방법이 없고, 초보자는 그 틀린 숫자를 믿는다.
  검증기가 `\d+배`, `부채비율 \d+` 같은 꼴을 걸러낸다.
- **주가 예측.** 원칙 5. "오를 것", "유망합니다" 같은 표현도 검증기가 막는다.

### 왜 미리 만들어 두는가

브라우저에서 AI 를 부를 수 없다. 정적 사이트라 키를 심으면 **소스에 그대로
노출된다.** 그래서 `analyze.yml` 이 하루에 몇 종목씩 만들어 `analysis.json` 에
커밋하고, 앱은 읽기만 한다(§16·§17과 같은 구조).

- 이미 만든 종목은 **다시 만들지 않는다.** 회사 설명은 자주 바뀌는 자료가
  아니고, 매번 다시 부르면 같은 회사가 날마다 다른 점수를 받는다.
- 아직 차례가 안 온 종목은 **없다고 말한다.** 지어내지 않는다.
- 유니버스 13종목은 사람이 쓴 카드를 먼저 쓴다. 화면에 어느 쪽인지 표시한다
  (`✍️ 직접 뜯어본` / `🤖 AI 가 정리`). 출처를 감추지 않는다.
- 파일이 크므로 **이 탭을 열 때만** 받는다.

## 22. 알림은 서버에서 보낸다 — 그리고 뜸해야 값어치가 있다

"모의 주식 변동이 생길 때 왜 그렇게 판단했는지와 함께 카카오톡으로 보내달라.
실시간 자동이 아니라 사용자가 알아야 즉각 수정하지 않겠나."

맞는 지적이고, 여기에는 구조적인 문제가 하나 있었다.

### 왜 서버에서 굴려야 했나

브라우저의 모의투자는 **앱을 열 때만** `simAutoTick` 으로 하루치를 돌린다.
즉 앱을 안 열면 **매매 자체가 일어나지 않아** 알릴 것도 생기지 않는다.
"앱을 안 열어도 알려주는" 알림을 브라우저가 만들 수는 없다.

그래서 `notify.yml` 이 **기준 계좌**(`sim-run.json`)를 서버에서 굴린다.
앱과 **같은 `sim.js` 엔진**을 Node 에서 그대로 불러 쓴다 — 로직을 두 벌로
만들면 알림 내용과 앱 화면이 서로 다른 말을 하게 된다.

### 개인 데이터는 여전히 안 나간다

기준 계좌는 **모델 포트폴리오**이지 회원님 계좌가 아니다. 보유 종목과 개인
모의계좌는 기기 밖으로 나가지 않는다는 원칙은 그대로다.

따라 투자하는 데 필요한 건 **내 잔고가 아니라 판단과 그 이유**다. 금액이
붙은 개인별 조언(§19)은 앱을 열면 그 자리에서 계산한다.

### 보내는 것 — 셋 다 "왜"를 같이 보낸다

| 언제 | 무엇을 |
|---|---|
| 국면이 바뀜 | **어느 항목이 움직였는지**와 그 판정 근거(실제 수치) + 현금 목표 변화 |
| 기준 계좌가 매매함 | 종목·금액·건수·평가액 + 조정한 이유 |
| 기사가 "근거 다시 확인" | 건수·제목 + "파라는 뜻이 아니다" 단서 |

"국면이 바뀌었습니다"만 오면 알림이 아니라 소음이다. **근거 없는 알림은
보내지 않는다.**

### 바뀐 게 없으면 아무것도 보내지 않는다

매일 "변화 없음"이 오면 이틀 만에 알림을 꺼 버린다. 알림의 값어치는 뜸한
데서 온다. 매일 도는 워크플로지만 **보내는 날은 드물어야 정상**이다.

### 카카오 제약

- **나에게 보내기(memo)만 쓴다.** 친구에게 보내기·알림톡은 검수와
  사업자등록이 필요해 개인 앱에서 못 쓴다. 어차피 본인이 받을 알림이다.
- **텍스트 템플릿은 200자까지.** 넘으면 그대로 거절당한다. `fit()` 이 줄
  단위로 자른다 — 문장 한가운데서 자르면 무슨 말인지 알 수 없다.
- 한 번의 재조정에서 나온 매매는 **이유가 같다.** 줄마다 반복하면 200자를
  넘겨 정작 중요한 평가액이 잘려 나간다. 공통 이유는 맨 아래 한 번만 적는다.
- 국기 이모지는 서로게이트 쌍 두 개다. `slice(3)` 으로 자르면 깨진다.
- **토큰 값은 어떤 로그에도 찍지 않는다.** 있는지 없는지와 길이만 남긴다.
- 시크릿이 없으면 **전송만 건너뛰고 내용은 로그에 남긴다** — 설정 전에
  무엇이 올지 먼저 볼 수 있다.

## 23. 성향은 시장이 아니라 내 사정으로 바꾼다

"시장이 좋아지면 공격적 투자로 바꿀 수 있으면 좋겠고, 그런 변화가 필요해
보이면 왜 필요한지 카톡으로 알려달라."

**바꾸는 기능은 만든다.** 여태 성향은 시작할 때 한 번 정하면 초기화 말고는
못 바꿨는데, 바꿔야 할 일은 실제로 생긴다 — 쓸 시점이 멀어졌거나, 소득이
늘어 견딜 수 있는 폭이 커졌거나.

**다만 "시장이 좋아 보여서" 옮기는 건 권하지 않는다.** 그건 오른 뒤에 더 사고
빠진 뒤에 줄이는, 정확히 반대로 가는 길이다. 그래서 고르는 자리와 알림 양쪽에
그 말을 붙인다 — **막지는 않되, 무엇을 하는 것인지는 알린다.**

### 시장에 맞추는 몫은 이미 현금 비중이 맡고 있다

이 앱에서 시장 국면에 반응하는 건 **현금 목표**다(`M.tilt`). 국면이 바뀌면
현금 목표가 자동으로 바뀌고, 기준 계좌가 거기에 맞춰 사고판다. 성향까지
시장에 따라 흔들면 같은 일을 두 번 하는 셈이고, 그 두 번째가 바로 고점 매수다.

그래서 `strategy` 알림은 **"공격적으로 바꾸세요"라고 말하지 않는다.**
"목표가 이만큼 달라졌고 이유는 이것"까지만 말하고, 성향을 바꿀지는 사용자가
정한다.

- 성향을 바꾸면 `lastAuto` 를 비워 **그 자리에서 새 목표로 다시 맞춘다.**
  안 그러면 내일까지 옛 목표로 남는다.
- 성향 변경은 `kind: 'note'` 로 **기록에 남긴다.** 계좌가 왜 달라졌는지는
  매매만으로 설명되지 않는다.

## 24. 알림은 읽히게 생겨야 한다

글자만 보내면 알림함에서 다른 메시지와 구분되지 않고, 눈에 안 들어오면 결국
안 읽는다. 안 읽는 알림은 없는 알림이다.

카카오 **feed 템플릿**을 쓴다 — 머리 이미지 + 제목 + 설명 + 버튼.

| 종류 | 배너 | 색 |
|---|---|---|
| `regime` 국면 변화 | 🧭 | 파랑 (브랜드) |
| `trade` 기준 계좌 조정 | 📒 | 초록 |
| `news` 근거 다시 확인 | 🗞️ | 주황 |
| `strategy` 전략 점검 | 🎚️ | 보라 |

- 배너는 `notify-img/` 에 **한 번만 만들어 커밋**한다(800×400, 카카오 권장 2:1).
  알림마다 이미지를 만들면 저장소가 계속 불어난다.
- 제목 60자, 설명 180자에서 자른다. 제목은 두 줄쯤에서 잘려 보이므로 짧게 쓰고
  본문은 설명에 담는다.
- 매매는 `🔴 매도` / `🔵 매수` 로 한 줄씩, 3건까지. 나머지는 "외 N건".
- 모든 알림에 **"왜"가 들어간다.** 근거 없는 알림은 보내지 않는다.

## 25. 홈은 전체를 본다. 시장을 고르는 화면이 아니다

모의투자 요약이 **지금 고른 시장 하나만** 보여줬다. 국내를 보는 동안 미국
계좌가 어떻게 됐는지 알 수 없었고, "내 모의투자 전체가 얼마"인지를 홈에서
알 수가 없었다. 두 계좌는 자동 운용으로 각자 굴러가므로 더 그렇다.

- 홈은 **합계를 위에**, 그 아래 **시장별 한 줄씩**. 국내 → 미국 순.
- 종목별 상세는 각 탭에서 본다. 홈에 다 펼치면 스크롤만 길어지고 결론이
  안 보인다.
- 시작하지 않은 시장도 "시작 안 함"으로 **자리를 남긴다.** 빠뜨린 것과
  안 한 것은 다르다.
- 뱃지(🆕 N건)도 두 시장을 합친다.
- 종목 줄은 **내 주식 위젯과 같은 `.mini` 행**을 쓴다. 상위 3종목 + "외 N종목".
  홈에서 두 위젯이 같은 말투로 읽혀야 "내 투자 vs 앱의 판단"을 나란히 볼 수
  있다 — 이 앱이 모의투자를 두는 이유가 그 비교다.
- 표시등(`simMark`)은 **목표 비중과의 차이**다. 내 주식의 판정 마크와 같은
  자리, 같은 역할이되 내용이 다르다: 자동 운용이 다음에 무엇을 건드릴지를
  미리 보여준다. 기준은 `SIM.band` 를 그대로 쓴다 — 화면과 실제 동작이
  다른 말을 하면 안 된다.

### 두 시장 금액을 그냥 더해도 되는 이유

`sim.js` 의 `priceOf` 가 국내는 원→만원, 미국은 달러→원→만원으로 바꿔
담는다. **둘 다 이미 원화(만원) 기준**이라 합산이 성립한다. 다른 곳에서
금액을 합칠 때도 이 단위를 먼저 확인한다.

### 시장별 줄은 한 줄로 고정한다

설명이 길어 두 줄이 되면 블록 높이가 들쭉날쭉해져서 국내·미국을 나란히
읽기 어려워진다. 설명은 `text-overflow: ellipsis`, 금액은 `nowrap`,
400px 이하에서는 글자만 줄인다. (실제로 "+831만"에서 "원"이 다음 줄로
떨어졌었다.)

## 26. 직장인이 따라 할 수 있는 조정이라야 한다

"직장인 투자자를 고려해 모의투자를 적정하게 조정하고, 무엇을 왜 조정했는지
카톡으로 보내달라. 그래야 그걸 보고 실제 계좌를 맞춰갈 수 있다."

받는 사람이 **실제로 실행할 수 있어야** 알림이 값어치가 있다. 여기에 두 가지
문제가 있었다.

### 금액이 아니라 비중으로 말한다

기준 계좌는 시드가 1,000만원이다. 받는 사람 계좌는 크기가 다르다.
`매수 KB금융 67만원` 은 그 사람 계좌에 옮길 수가 없다.

> 🔵 매수 KB금융 **5.6% → 9%**

비중은 500만원 계좌에서도 5,000만원 계좌에서도 뜻이 같다. 그래서 `plan()` 이
조정 계획에 `w`(지금 비중)·`wT`(목표 비중)를 **값으로** 담는다 — 문자열
`why` 안에 묻혀 있으면 알림에서 꺼내 쓸 수 없다.

### 주 1회로 모은다. 다만 국면이 바뀐 날은 기다리지 않는다

매일 조정하면 계좌는 목표에 딱 붙지만, 그걸 실제 계좌로 옮기는 사람은 매일
사고팔아야 한다. 직장인에게는 불가능하고, **이 앱의 실수 목록에도 "매일
계좌를 본다"가 들어 있다.** 앱이 자기 말을 뒤집으면 안 된다.

`sim.js` 의 `dueToday()`:

| 언제 | 조정하나 |
|---|---|
| 금요일 | ✅ |
| 국면이 바뀐 날 | ✅ 기다리지 않는다 — 국면이 곧 목표를 바꾸는 값이다 |
| 마지막 조정에서 7일 지남 | ✅ (금요일이 장 안 열린 날이었을 때) |
| 그 밖의 평일 | ❌ |

**주기는 앱과 알림이 같은 값을 쓴다**(`cadence: 'weekly'`). 한쪽만 매일 돌면
알림과 앱 화면이 서로 다른 계좌를 보게 된다.

## 27. 투자 금액은 도중에 바뀐다 — 그때가 가장 배울 자리다

"모의투자 금액도 중간에 조정이 가능했으면 한다. 금액이 바뀌면 그에 맞게
매도나 추가 매수가 진행됐으면 하고, 그래야 **어떤 주식을 팔아야 하고 어떤
주식을 가지고 가고 어떤 주식을 더 사는 게 좋은지** 알 수 있다."

실제로 넣을 수 있는 돈은 달라진다. 상여가 들어오기도 하고 목돈이 필요해
빼기도 한다. 여태 시드는 시작할 때 정하면 초기화 말고는 못 바꿨다.

### 넣고 뺀 돈은 손익이 아니다

`pl = total − seed` 이므로, **cash 와 seed 를 같이 움직여야 한다.** 돈만
넣고 seed 를 그대로 두면 넣은 돈이 통째로 수익으로 잡힌다. 500만원을 넣어
수익률이 +50%가 되는 계좌는 아무것도 가르쳐주지 않는다.

### 뺄 때 무엇을 파는가 — 이게 이 기능의 핵심이다

`raiseCash()` 는 **목표 비중에서 가장 많이 벗어난 자리부터** 판다. 오른
종목은 저절로 비중이 커져 있으므로, 자연히 "비싸진 것부터 덜어내는" 순서가
된다. 균등하게 나눠 파는 것보다 목표에 가깝고, 무엇보다 **왜 그 종목인지를
설명할 수 있다.**

- 넣으면 목표 비중에 맞춰 나눠 담는다.
- 빼면 현금이 모자란 만큼만 판다. 현금으로 충당되면 아무것도 팔지 않는다.
- 전체 평가금액보다 많이 빼려 하면 **막고, 얼마까지 가능한지 알려준다.**
- 시세를 못 받아온 종목은 손대지 않는다 — 그래서 다 못 팔면 실패로 되돌린다.
- 바꾼 뒤 `lastAuto` 를 비워 **곧바로** 목표에 맞춘다. 다음 금요일까지
  기다리면 "돈을 넣었는데 아무 일도 안 일어나는" 상태가 된다(§26).
- 입출금은 `kind: 'cash'` 로 기록에 남는다. 계좌가 왜 달라졌는지는 매매만으로
  설명되지 않는다.

## 아키텍처 원칙

- **정적 사이트, 빌드 도구 없음.** GitHub Pages가 파일을 그대로 서빙한다.
  npm 스크립트, 번들러, 트랜스파일러를 도입하지 않는다.
- 프레임워크 없음. 바닐라 JS, ES5 문법 범위를 유지한다(구형 인앱 브라우저 대응).
- 파일은 5개로 유지한다. 새 기능은 기존 파일에 얹지 말고 분리된 파일로 만든다.

  | 파일 | 역할 | 전역 |
  |---|---|---|
  | `config.js` | 접근 코드 설정 | `window.BCConfig`, `window.BCHash` |
  | `data.js` | 블루칩 유니버스·ETF·용어·세금·실수·로드맵 | `window.BCData` |
  | `market.js` | 국면 다이얼·판정 규칙·구조적 테마 | `window.BCMarket` |
  | `portfolios.js` | 성향별 모델 포트폴리오 | `window.BCPortfolios` |
  | `holdings.js` | 보유 종목 판정 규칙 | `window.BCHoldings` |
  | `sim.js` | 모의투자 엔진(매수·매도·평가·비중 추적) | `window.BCSim` |
  | `live.json` | 시세 스냅샷 (**사람이 손대지 않는다** — 워크플로가 씀) | — |
  | `tickers.json` | 상장 종목 이름 색인 (같음, 폼 열 때만 로드) | — |
  | `scripts/judge-regime.mjs` | 국면 자동 판정(규칙 + AI). 워크플로에서만 실행 | — |
  | `scripts/judge-news.mjs` | 기사 판정(규칙 + AI). 워크플로에서만 실행 | — |
  | `scripts/price-list.mjs` | 시세만 받아오는 종목 목록 (**평가 대상 아님**) | — |
  | `prices.json` | 그 목록의 시세 (**보유가 있을 때만** 로드) | — |
  | `app.js` | 화면 로직 | (없음, IIFE) |
  | `app.css` | 전체 스타일 | — |
  | `index.html` | 껍데기 | — |

- **모델 포트폴리오 규칙**(`portfolios.js`): 비중 합은 항상 100이고 현금도 한
  자리를 차지한다(현금을 "남은 것"으로 두면 사용자가 전액을 주식에 넣는다).
  종목은 7~8개로 묶는다 — 더 늘리면 분산이 아니라 방치가 된다. 자리마다 "왜 이
  자리에 이게 있는지"를 한 줄로 적는다. 이유 없는 목록은 그냥 추천이 된다.
  **국면은 현금 비중만 움직인다** — 성향별 위험도는 이미 종목 구성에 반영돼
  있어서 이중으로 걸면 같은 위험을 두 번 뺀다.
- **주수·가격을 계산하지 않는다.** 주가를 모르는데 아는 척하면 안 된다.
  금액만 주고 증권사 앱의 금액 주문으로 넘긴다.
- `[hidden]`은 브라우저 기본 스타일이라 클래스가 `display`를 지정하면 진다.
  `app.css` 상단의 `[hidden] { display: none !important; }`를 지우지 말 것 —
  없으면 숨긴 전체화면 오버레이가 투명하게 남아 클릭을 전부 삼킨다.

- **localStorage 키는 전부 `bcc:` 접두사.** 기존 키를 삭제하거나 구조를 바꾸지
  않는다. 사용자가 명시적으로 요청한 "기록 초기화" 외에는 데이터를 지우지 않는다.
  저장한 값은 이 브라우저에만 남고 서버로 전송하지 않는다. 이 앱은 계정도,
  분석 스크립트도, 외부 요청도 두지 않는다.
## 공개 범위

이 앱은 **만든 사람과 지인만 쓰는 앱**이다.

- `index.html`의 `<meta name="robots" content="noindex, nofollow">`와
  `robots.txt`의 `Disallow: /`를 **함께** 유지한다. 하나만 있으면 안 된다 —
  robots.txt는 크롤링을, meta는 색인을 막는다. `sitemap.xml`은 두지 않는다.
- `config.js`의 접근 코드는 **보안이 아니라 문패다.** 정적 호스팅이라 소스를
  열면 보인다. 그러니 **비밀로 지켜야 할 것을 앱에 넣지 않는다** — 계좌번호,
  실제 보유 종목, 실제 투자 금액을 저장하거나 전송하는 기능을 만들지 않는다.
  입력한 시드조차 이 브라우저 밖으로 나가지 않는다.
- 로그인·계정·서버 저장을 도입하지 않는다. 필요해지면 그건 다른 제품이다.

## 색과 아이콘

- 팔레트: 스카이블루 배경 `#e9f2fb` / 딥네이비 글자 `#13334f` / 오션블루 강조
  `#0e6ba8`. 시장 전환 시 강조색만 오션블루↔인디고 `#3d3f9e`로 바뀌어, 글자를
  읽지 않아도 어느 시장을 보는지 알게 한다.
  **화면마다 새 팔레트를 만들지 않는다.**
- **빨강 `--up: #e5342b`(주식 상승의 빨강)은 아껴 쓴다.** 나침반 북쪽 바늘,
  "제안"의 위성 비중, 그리고 손익의 플러스 표시 — 이 셋뿐이다. 화면에 빨강이
  흔해지면 그게 곧 "지금 뭔가 하라"는 신호가 되고, 그 순간 이 앱은 원칙 6과
  충돌한다.
- **손익 표시는 부호와 텍스트 색까지만.** 한국 관행대로 플러스는 빨강(`--up`),
  마이너스는 파랑(`--accent`)을 쓰되 **글자 색에만** 적용한다. 배경색, 큰 강조
  블록, 깜빡임, 등락 화살표 애니메이션은 만들지 않는다. 화면이 등락에 반응하기
  시작하면 감정 매매를 줄인다는 목적과 정면으로 충돌한다.
- 앱 아이콘은 **나침반**이다. 스카이블루 배경 위에 북쪽 바늘만 빨강으로 찍는다.
  상승 화살표·돈·차트를 주 모티프로 쓰지 않는다 — "수익률을 약속하지 않는다"는
  원칙의 연장이다. 화면 상단 브랜드 마크도 이모지가 아니라 **같은 나침반 SVG**를
  인라인으로 쓴다(설치한 아이콘과 화면이 같은 그림이어야 한다).
  아이콘을 바꿀 때는 `icon.svg`(파비콘/Android any), `icon-maskable.svg`,
  `icon-180.png`(iOS), `icon-192.png`/`icon-512.png`(Android any),
  `icon-512-maskable.png`(adaptive, 안전 영역 확보) **여섯 개를 모두** 다시
  만든다(만드는 방법은 README 참고). 상단 브랜드 마크의 인라인 SVG도 같이 고친다.

## 작업 방식

작업 전 실제 코드를 읽고, 작업 후에는 변경 파일 / 신규 파일 / 변경 기능 /
기존 기능에 미친 영향 / 테스트한 항목 / 발견된 문제 / 다음 단계를 보고한다.
구조를 크게 바꿔야 하는 문제를 발견하면 먼저 보고하고 승인을 받은 뒤 진행한다.
